package main

func main() {
	// aritméticos
	const (
		soma          = 1 + 2
		subtracao     = 1 - 2
		multiplicacao = 1 * 2
		divisao       = 4 / 2
		resto         = 5 % 2
	)

	// relacionais
	const (
		maior       = 1 > 2
		menor       = 1 < 2
		maiorIgual  = 1 >= 2
		menorIgual  = 1 <= 2
		diferente   = 1 != 2
		equivalente = 1 == 2
	)

	// lógicos
	const (
		and = true && false
		or  = true || false
		not = !true
	)

	// atribuição
	x := 1
	y := 2
	x += y
	x -= y
	x *= y
	x /= y
	x %= y
	x++
	x--

	// bit a bit
	const ()
}
